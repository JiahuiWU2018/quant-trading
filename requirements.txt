# ============================================================
# quant-trading — core runtime dependencies
# ============================================================
# Install:  pip install -r requirements.txt
# For dev:  pip install -r requirements-dev.txt
# ============================================================

# --- Numeric & data ---
numpy>=1.26
pandas>=2.0
scipy>=1.11
pyarrow>=14.0          # parquet cache layer

# --- Market data (Phase 1) ---
yfinance>=0.2
# Fama-French data is fetched directly via HTTPS (requests) — no pandas_datareader needed

# --- Backtesting (Phase 2) ---
backtrader>=1.9.78

# --- Portfolio optimisation (Phase 4) ---
cvxpy>=1.4
scikit-learn>=1.3          # Ledoit-Wolf covariance estimation (Phase 4)
osqp>=0.6              # default CVXPY solver (no license required)
ecos>=2.0              # fallback solver
scs>=3.0               # fallback solver
# mosek is optional — install separately if you have a license:
#   pip install mosek>=10.0

# --- Live execution / IBKR (Phase 3) ---
ib_insync>=0.9.86
